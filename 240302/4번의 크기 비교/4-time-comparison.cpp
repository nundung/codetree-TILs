#include <iostream>
using namespace std;
int main() {
    int a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;
    cout << (a > b) << '\n' << (a > c) << '\n' << (a > d) << '\n' << (a > e);
    // 여기에 코드를 작성해주세요.
    return 0;
}