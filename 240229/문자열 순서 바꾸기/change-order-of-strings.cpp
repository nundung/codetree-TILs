#include <iostream>
using namespace std;
int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    string s, t;
    cin >> s >> t;
    cout << t << endl << s;
    // 여기에 코드를 작성해주세요.
    return 0;
}