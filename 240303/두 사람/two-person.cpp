#include <iostream>
using namespace std;
int main() {
    int a, c;
    char b, d;
    cin >> a >> b >> c >> d;
    cout << ( (a >= 19 && b == 'M') || (c >= 19 && d == 'M') );
    // 여기에 코드를 작성해주세요.
    return 0;
}